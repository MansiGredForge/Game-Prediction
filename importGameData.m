function steamtrain = importGameData(filename, dataLines)
%IMPORTFILE Import data from a text file
%  STEAMTRAIN = IMPORTFILE(FILENAME) reads data from text file FILENAME
%  for the default selection.  Returns the data as a table.
%
%  STEAMTRAIN = IMPORTFILE(FILE, DATALINES) reads data for the specified
%  row interval(s) of text file FILENAME. Specify DATALINES as a
%  positive scalar integer or a N-by-2 array of positive scalar integers
%  for dis-contiguous row intervals.
%
%  Example:
%  steamtrain = importfile("F:\Logical Rythm\Game Predictor\steam_train.csv", [2, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 03-Oct-2020 17:12:02

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [2, Inf];
end

%% Setup the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 18);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = ",";

% Specify column names and types
opts.VariableNames = ["appid", "name", "release_date", "english", "developer", "publisher", "platforms", "required_age", "categories", "genres", "steamspy_tags", "achievements", "positive_ratings", "negative_ratings", "average_playtime", "median_playtime", "owners", "price"];
opts.VariableTypes = ["double", "string", "datetime", "double", "string", "string", "categorical", "double", "categorical", "categorical", "string", "double", "double", "double", "double", "double", "categorical", "double"];

% Specify file level properties
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";

% Specify variable properties
opts = setvaropts(opts, ["name", "developer", "publisher", "steamspy_tags"], "WhitespaceRule", "preserve");
opts = setvaropts(opts, ["name", "developer", "publisher", "platforms", "categories", "genres", "steamspy_tags", "owners"], "EmptyFieldRule", "auto");
opts = setvaropts(opts, "release_date", "InputFormat", "yyyy-MM-dd");

% Import the data
steamtrain = readtable(filename, opts);

end